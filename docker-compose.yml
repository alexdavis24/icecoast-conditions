services:
  backend:
    build:
      context: ./src/backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./src/frontend
    depends_on:
      - backend
    ports:
      - "3000:3000"
